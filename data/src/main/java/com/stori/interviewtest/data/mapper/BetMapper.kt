package com.stori.interviewtest.data.mapper

import com.google.firebase.auth.FirebaseUser
import com.stori.interviewtest.data.UserStori
import com.stori.interviewtest.data.models.Movement
import javax.inject.Inject

class BetMapper @Inject constructor() {
    /*fun fromResponseToDomain(bookResponse: FirebaseUser): Movement {
        return Movement(
            type = bookResponse.type,
            description = bookResponse.type,
            total = bookResponse.odds,
        )
    }*/
}
